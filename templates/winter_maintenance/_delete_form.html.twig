<form method="post" action="{{ path('winter_maintenance_delete', {'id': winter_maintenance.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ winter_maintenance.id) }}">
    <button class="btn">Delete</button>
</form>