{% extends 'base.html.twig' %}
{% trans_default_domain 'translation' %}

{% block title %}{% endblock %}

{% block body %}

    <h3>Redaguoti</h3>

    {{ include('winter_jobs/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-info btn-block btn-lg" href="{{ path('winter_jobs_index') }}">{% trans %}back to list{% endtrans %}</a>

    {{ include('winter_jobs/_delete_form.html.twig') }}

    <br>
{% endblock %}