<form method="post" action="{{ path('insured_event_delete', {'id': insured_event.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ insured_event.id) }}">
    <button class="btn btn-lg btn-block btn-danger">{% trans %}Delete{% endtrans %}</button>
</form>