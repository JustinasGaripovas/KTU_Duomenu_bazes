{% extends 'base.html.twig' %}

{% block title %}DoneJobs{% endblock %}

{% block body %}
    <h1>DoneJobs</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>JobId</th>
                <td>{{ done_job.JobId }}</td>
            </tr>
            <tr>
                <th>JobName</th>
                <td>{{ done_job.JobName }}</td>
            </tr>
            <tr>
                <th>RoadSection</th>
                <td>{{ done_job.RoadSection }}</td>
            </tr>
            <tr>
                <th>RoadSectionBegin</th>
                <td>{{ done_job.RoadSectionBegin }}</td>
            </tr>
            <tr>
                <th>RoadSectionEnd</th>
                <td>{{ done_job.RoadSectionEnd }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ done_job.Quantity }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ done_job.Username }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ done_job.Date ? done_job.Date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>DoneJobDate</th>
                <td>{{ done_job.DoneJobDate ? done_job.DoneJobDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('done_jobs_index') }}">back to list</a>

    <a href="{{ path('done_jobs_edit', {'id': done_job.id}) }}">edit</a>

    {{ include('done_jobs/_delete_form.html.twig') }}
{% endblock %}