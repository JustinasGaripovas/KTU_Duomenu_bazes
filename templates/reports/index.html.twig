{% extends 'base.html.twig' %}
{% trans_default_domain 'translation' %}

    {% block body %}
        <script>
            $( function() {
                $( ".js-datepicker-1" ).datepicker();
                $( ".js-datepicker-2" ).datepicker();
            });
        </script>
        <style>
            @media print {
                .print_a {
                    overflow: visible !important;
                }
            }
            thead { display: table-header-group; }
            tr{page-break-inside: avoid !important;}
        </style>

            {{ include('/reports/form.html.twig') }}
        <br>
        <hr>

        <div class="print_a">
            <table class="table table-sm thead-dark table-bordered">
                <thead>
                <tr>
                    <th style="text-align: center; vertical-align: middle">{% trans %} JobId {% endtrans %}</th>
                    <th style="text-align: center; vertical-align: middle">{% trans %} JobName  {% endtrans %}</th>
                    <th style="text-align: center; vertical-align: middle">{% trans %} RoadSection {% endtrans %}</th>
                    <th style="text-align: center; vertical-align: middle">{% trans %} RoadSectionBegin {% endtrans %}</th>
                    <th style="text-align: center; vertical-align: middle">{% trans %} RoadSectionEnd {% endtrans %}</th>
                    <th style="text-align: center; vertical-align: middle">{% trans %} Unit of {% endtrans %}</th>
                    <th style="text-align: center; vertical-align: middle">{% trans %} Quantity {% endtrans %}</th>
                    <th style="text-align: center; vertical-align: middle">{% trans %} username {% endtrans %}</th>
                    <th style="text-align: center; vertical-align: middle">{% trans %} DoneJobDate {% endtrans %}</th>
                    <th style="text-align: center; vertical-align: middle">{% trans %} Comments  {% endtrans %}</th>
                </tr>
                </thead>
                <tbody>
                {% for done_job in report | default ('') %}
                    <tr {% if loop.index is odd %}class="color"{% endif %}>
                    <tr>
                        <td style="text-align: center; vertical-align: middle">{{ done_job.JobId }}</td>
                        <td style="text-align: center; vertical-align: middle">{{ done_job.JobName }}</td>
                        <td style="text-align: center; vertical-align: middle">{{ done_job.RoadSection }}</td>
                        <td style="text-align: center; vertical-align: middle">{{ done_job.RoadSectionBegin }}</td>
                        <td style="text-align: center; vertical-align: middle">{{ done_job.RoadSectionEnd }}</td>
                        <td style="text-align: center; vertical-align: middle">{{ done_job.UnitOf }}</td>
                        <td style="text-align: center; vertical-align: middle">{{ done_job.Quantity }}</td>
                        <td style="text-align: center; vertical-align: middle">{{ done_job.Username }}</td>
                        <td style="text-align: center; vertical-align: middle; white-space: nowrap">{{ done_job.DoneJobDate ? done_job.DoneJobDate|date('Y-m-d') : '' }}</td>
                        <td style="text-align: center; vertical-align: middle">{{ done_job.Note }}</td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="13">{% trans %}no records found{% endtrans %}</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
            ________________________________________________________________________________________________
        </div>
    {% endblock %}
