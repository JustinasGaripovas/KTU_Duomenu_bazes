<form method="post" action="{{ path('flooded_roads_delete', {'id': flooded_road.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ flooded_road.id) }}">
    <button class="btn">Delete</button>
</form>
